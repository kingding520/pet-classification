# 图片分类设计与开发后续工作步骤

## 调整后的工作目标

后续工作保留 GPU 训练与端侧部署路线：阶段二记录 MobileNetV2 GPU 环境的兼容性验证；阶段三起使用 ResNet-18 完成兼容 GPU 主机训练、同架构 CPU 对照、MindIR 导出、MindSpore Lite `.ms` 生成和 Android 验证。

不再使用 ShuffleNetV2，也不再开展 ImageNet ILSVRC 2012 子集训练。

## 阶段一：固化当前成果（已完成）

1. 已完成 MobileNetV2 代码、训练配置、模型文件和手机端模型的基线快照。
2. 已在 CPU 环境复现 MobileNetV2 训练、MindIR 导出和 MindSpore Lite 转换流程。
3. 已完成手机端 APP 实机识别验证。

**已完成结果：** MobileNetV2 CPU 基线训练验证准确率最高为 99.49%；手机端模型与复现转换产物的 SHA-256 一致。

## 阶段二：MobileNetV2 GPU 环境验证（已阻塞）

### 目标

使用 GPU 对现有猫狗数据集训练 MobileNetV2，并保存训练检查点、运行日志、环境信息和验证结果。

### 工作步骤

1. 确认可用 GPU 训练主机及其 Linux、Python、CUDA、cuDNN、NVIDIA 驱动和 MindSpore 版本。
2. 建立独立 GPU 环境，避免改动已验证的 Windows CPU 基线环境和手机端模型。
3. 运行 MindSpore GPU 可用性检测与简单张量计算，确认计算实际落在 GPU 上。
4. 使用现有 PetImages 猫狗数据集和 MobileNetV2 训练代码完成单批次前向、反向传播及检查点写入测试。
5. 使用确定的训练参数启动正式 GPU 训练，持续记录损失、验证准确率、显存占用、单 epoch 耗时和异常日志。
6. 保存最终或验证表现最好的 `.ckpt`，在独立验证集上执行一次评估，并归档模型哈希与完整运行命令。

### 验收标准

- MindSpore 能识别并使用目标 GPU。
- MobileNetV2 至少完成一个 GPU 训练步，且可成功写入 `.ckpt`。
- 正式训练完成后可获得可加载的 GPU 训练检查点及验证结果。
- 实验记录可复现环境、数据路径、训练参数和结果。

## 当前环境限制

本机 RTX 5060 Laptop GPU、CUDA 13.2、WSL Ubuntu 24.04 不属于原计划 MindSpore 2.2.14 与 CUDA 11.x 的受支持组合。因此，GPU 正式训练应使用经过兼容性验证的 Linux GPU 主机，或先在完全隔离的旧版环境中通过 GPU 冒烟测试后再继续。

截至 2026-09-13 的实测中，WSL 独立环境内的 MindSpore 2.10.0 在设置 `device_target=GPU` 时因缺少 CUDA 11.x 与 cuDNN 8 动态库而失败，框架仅支持 CPU。故本机尚未执行 GPU 训练步，也没有可与 CPU 基线（30 epoch、最高验证准确率 99.49%、总耗时 17.25 s）比较的 GPU 准确率或耗时数据。

## 阶段三：ResNet-18 GPU 训练（已完成）

**执行状态（2026-09-14）：** 已使用 PyTorch CUDA 12.8 在本机 RTX 5060 完成 30 epoch ResNet-18 训练。最佳验证准确率为 74.24%（第 26 epoch），总耗时 200.10 s，峰值显存 891,883,008 B；最佳 `.pth` 及日志已归档。MindSpore GPU 环境仍不兼容，但不影响后续以 MindSpore Lite Converter 从 ONNX 生成 `.ms`。

### 架构选择

使用 ResNet-18 替代已取消的 ShuffleNetV2 路线。ResNet-18 的残差结构成熟，适合作为猫狗二分类的 GPU 训练与端侧部署候选。由于本机 MindSpore GPU 与 RTX 5060/CUDA 13.2 不兼容，正式训练使用 PyTorch CUDA；后续仍使用 MindSpore Lite Converter 生成端侧 `.ms` 模型。

### 工作步骤

1. 固定 PyTorch CUDA、驱动、Python、torchvision 与 MindSpore Lite Converter 版本；训练仅在 CUDA 实际可用时启动。
2. 在独立目录实现 ResNet-18 猫狗二分类训练脚本，使用与 CPU 基线一致的 PetImages 数据划分、类别映射、随机种子 1、批大小 32 与 30 epoch。
3. 在 GPU 上完成张量计算、单批次前向/反向与 `.pth` 写入冒烟测试，并保存 `nvidia-smi` 采样、显存峰值和日志。
4. 启动正式 GPU 训练，按 epoch 记录训练损失、验证准确率、单 epoch 耗时与最佳检查点。

### 验收标准

- ResNet-18 至少完成一个真实 CUDA GPU 训练步，日志证明使用 RTX 5060。
- 获得可加载的最佳 `.pth`、完整训练日志、版本清单和模型 SHA-256。

## 阶段四：CPU 对照与结果比较（已完成）

**执行状态（2026-09-14）：** 同架构 CPU 训练已完成 30 epoch。GPU/CPU 最佳验证准确率分别为 74.24% 与 72.22%，总耗时分别为 200.10 s 与 1682.46 s；GPU 训练耗时为 CPU 的 1/8.41。完整对照见 `experiments/resnet18_gpu_2026-09-14/阶段四执行记录.md`。

1. 以相同 ResNet-18 代码、数据划分、随机种子和训练参数在 CPU 后端重复一次训练，形成 GPU/CPU 的同架构对照。
2. 分别记录最高验证准确率、总耗时、平均每 epoch 耗时、峰值显存/内存和最佳检查点大小。
3. 保留既有 MobileNetV2 CPU 基线（99.49%、17.25 s）作为项目历史参考；不将它与 ResNet-18 GPU 的时间差直接表述为“GPU 加速倍数”，因为硬件和网络结构均不同。
4. 将 ResNet-18 的 GPU/CPU 同架构对照作为正式速度结论，并单列 MobileNetV2 与 ResNet-18 的准确率、模型体积和端侧结果差异。

## 阶段五：ONNX 导出与 MindSpore Lite `.ms` 生成（已完成）

**执行状态（2026-09-14）：** GPU 最佳 `.pth` 已导出为 ONNX（固定输入 `[1,3,224,224]`），并通过 ONNX Runtime 与 PyTorch 输出一致性验证（最大绝对误差 `2.38e-7`）。MindSpore Lite 2.9 Converter 已成功生成 `resnet18_pet.ms`（44,703,832 B）；详细记录见 `experiments/resnet18_gpu_2026-09-14/阶段五执行记录.md`。

1. 加载 ResNet-18 最佳 `.pth`，切换推理模式，使用固定输入张量（形状 `[1, 3, 224, 224]`）导出 `resnet18_pet.onnx`。
2. 使用 ONNX Runtime 或 PyTorch 独立进程加载 ONNX 并对固定样本推理，确认导出前后类别索引、置信度排序一致。
3. 使用与 Android 端运行时匹配的 MindSpore Lite Converter 从 ONNX 生成 `.ms`：

```powershell
converter_lite --fmk=ONNX --modelFile=resnet18_pet.onnx --outputFile=resnet18_pet
```

4. 校验转换输出包含 `CONVERT RESULT SUCCESS:0`，记录 `.onnx`、`.ms` 的大小和 SHA-256；若端侧模型体积过大，再单独评估量化，不覆盖浮点基线。

**说明：** 本机 GPU 训练采用 PyTorch CUDA；MindSpore Lite Converter 负责将 ONNX 转换为 `.ms` 端侧模型格式。

### 验收标准

- `resnet18_pet.onnx` 可独立加载推理。
- Converter 成功生成 `resnet18_pet.ms`，并完成哈希校验。

## 阶段六：Android 集成与端侧验证

1. 将 `resnet18_pet.ms` 放入 Android App 模型目录，并使 App 的 MindSpore Lite 运行时与 Converter 版本匹配；必要时先升级 App 依赖后再替换模型。
2. 在同一手机上执行猫、狗各不少于 10 张独立测试图片，记录预测类别、置信度、推理耗时、模型加载结果和崩溃日志。
3. 与 MobileNetV2 端侧基线按相同测试集比较准确率、模型体积和平均推理耗时；若 ResNet-18 不满足端侧体积或时延要求，保留结果并将模型压缩作为后续独立优化项。

## 阶段七：结题材料整理

1. 汇总 GPU/CPU 同架构对照、MobileNetV2 历史基线、ResNet-18 训练曲线、模型哈希、ONNX/.ms 转换日志和手机实测表。
2. 明确写出本机 RTX 5060 的 MindSpore GPU 冒烟失败原因，以及正式 GPU 结果所用兼容主机环境，避免混淆。
3. 归档训练脚本、参数、环境清单、最佳 `.pth`、`.onnx`、`.ms` 和复现实验命令。

## 已停止的路线

- ShuffleNetV2 训练、评估、转换和部署不再继续。
- ImageNet ILSVRC 2012 子集准备与训练不再继续。

## 风险与处理原则

- 不以 CPU 训练替代 GPU 训练结果；GPU 环境未通过冒烟测试时，不进入正式训练。
- 若当前本机无法通过 GPU 冒烟测试，转移到兼容 GPU 主机，不覆盖现有 CPU 基线或 Android 端模型。
- 每次环境调整均记录完整版本信息和报错日志，便于定位 CUDA、cuDNN、驱动和 MindSpore 的兼容问题。
