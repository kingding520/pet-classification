# 图片分类设计与开发项目结题材料汇总

> 整理日期：2026-09-14  
> 项目状态：训练、转换与 ResNet-18 真机运行时验证已完成；新版 Android App 集成与 20 张独立图片测试待 Android 源码后继续完成。

## 1. 项目目标与完成情况

项目完成了猫狗二分类模型的训练、验证、模型导出和端侧运行时验证，形成 MobileNetV2 历史基线与 ResNet-18 GPU 训练路线两组可追溯成果。

| 目标 | 状态 | 证据 |
| --- | --- | --- |
| 固化原有 MobileNetV2 CPU 基线 | 已完成 | 30 epoch、最高验证准确率 99.49%、端侧 App 识别通过 |
| MindSpore GPU 环境验证 | 已完成（不兼容） | RTX 5060 / CUDA 13.2 无法加载 MindSpore CUDA 11.x GPU 插件，保留完整报错与环境记录 |
| 替代架构 GPU 训练 | 已完成 | PyTorch CUDA 下完成 ResNet-18 30 epoch 训练 |
| 同架构 GPU/CPU 对照 | 已完成 | 相同 ResNet-18、数据、参数与 30 epoch 条件下 GPU 快 8.41 倍 |
| ONNX 与 MindSpore Lite `.ms` 生成 | 已完成 | ONNX 输出与 PyTorch 最大绝对误差 `2.384e-7`；Converter 成功 |
| Android 真机模型运行时验证 | 已完成 | Lite 2.9 Android benchmark 成功加载并推理，平均 35.941 ms |
| 新版 App 集成与 20 张独立图片测试 | 待完成 | 当前工作区无 Android 源码，旧 App 的 Lite 2.0 运行时不应直接加载 Lite 2.9 模型 |

## 2. 主要结果

### 2.1 MobileNetV2 历史 CPU 基线

| 项目 | 结果 |
| --- | --- |
| 框架 / 后端 | MindSpore 2.9.0 / Windows CPU |
| 数据划分 | 训练集 Cat 895、Dog 894；验证集 Cat 99、Dog 99 |
| 训练 | 30 epoch |
| 最高验证准确率 | **99.49%** |
| 总训练耗时 | **17.25 s** |
| 手机上的已有 App | `com.mindspore.classificationforpet` 1.1，可启动并完成一次图片识别 |
| 端侧模型 | `pet.ms`，8,888,232 B |

该基线用于说明原有模型与手机端流程可复现。它与 ResNet-18 的模型结构和运行环境不同，**不得**据此计算 GPU 加速倍数。

### 2.2 ResNet-18 同架构 GPU/CPU 对照（正式速度结论）

对照条件：同一 WSL 主机、PyTorch 2.11.0、PetImages 数据划分、随机种子 1、批大小 32、学习率 0.01、无预训练权重、30 epoch。

| 指标 | GPU | CPU | 结论 |
| --- | ---: | ---: | --- |
| 实际后端 | `cuda:0`（RTX 5060 Laptop GPU） | `cpu` | 显式指定，无回退 |
| 最佳验证准确率 | 74.24%（第 26 epoch） | 72.22%（第 28 epoch） | GPU 高 2.02 个百分点 |
| 总训练耗时 | 200.0957 s | 1682.4581 s | **GPU 快 8.41 倍** |
| 平均每 epoch | 6.67 s | 56.08 s | **GPU 快 8.41 倍** |
| 峰值 GPU 显存 | 891,883,008 B（约 850 MiB） | 0 B | CPU 未调用 GPU |

### 2.3 导出和端侧运行时验证

| 项目 | 结果 |
| --- | --- |
| GPU 最佳检查点 | `resnet18_best_epoch26.pth`，最佳验证准确率 74.24% |
| ONNX 导出 | 固定输入 `[1,3,224,224]`、Float32、opset 17 |
| 类别映射 | `Cat=0`，`Dog=1` |
| PyTorch / ONNX Runtime 最大绝对误差 | `2.384185791015625e-07` |
| Lite 转换 | MindSpore Lite 2.9 Converter，`CONVERT RESULT SUCCESS:0` |
| ResNet-18 `.ms` 大小 | 44,703,832 B（约为旧 MobileNetV2 App 模型的 **3.86 倍**） |
| 真机芯片 / ABI | SM8650 / `arm64-v8a` |
| 真机运行时 | MindSpore Lite 2.9 Android-aarch64 benchmark，CPU 2 线程 |
| 真机模型准备 | 232.418 ms |
| 真机推理 | 3 次预热、10 次测试；平均 **35.941 ms**，结果成功 |

## 3. 环境与技术路线说明

原计划 MindSpore GPU 路线未在本机执行正式训练。经实测，本机是 RTX 5060 Laptop GPU、CUDA 13.2、WSL Ubuntu 24.04；MindSpore GPU wheel 所需 CUDA 11.1/11.6 与 cuDNN 8 动态库缺失，GPU 后端初始化失败。因此，正式 GPU 训练改用兼容本机 CUDA 12.8 的 PyTorch；模型随后经 ONNX 转换为 MindSpore Lite `.ms`。

该调整的边界如下：

- GPU 训练结论来自 PyTorch CUDA，非 MindSpore GPU；
- `.ms` 生成来自 MindSpore Lite 2.9 Converter；
- Android 真机加载使用同版本 Lite 2.9 benchmark；
- 已安装的旧 Android App 内置 Lite 2.0，不能直接用文件替换方式植入 Lite 2.9 模型。

## 4. 关键文件与哈希

| 产物 | 位置 | SHA-256 |
| --- | --- | --- |
| ResNet-18 GPU 最佳权重 | `experiments/resnet18_gpu_2026-09-14/outputs/gpu_30ep_v2/resnet18_best_epoch26.pth` | `97fec46316eb979cab861bc44d2c71b07f580eb6394daf7f12532eaa9e57d884` |
| ONNX 模型 | `experiments/resnet18_gpu_2026-09-14/exports/onnx_mslite_2026-09-14/resnet18_pet.onnx` | `75110abf18efc078f4ea6521bca2bbb3cd2a363ff6fd30051e7ca63578e9ad88` |
| Lite 模型 | `experiments/resnet18_gpu_2026-09-14/exports/onnx_mslite_2026-09-14/resnet18_pet.ms` | `19dc76d6de82c1e559e312bdccdec0a836bd40b5f97e93d576f95443539d661a` |
| MobileNetV2 App 模型归档 | `artifacts/baseline-2026-09-13/source_snapshot/deployment/pet.ms` | `7ac8f7e0078e351d4e5a2241a19354c85b08624e786c5e7e33e7a9b917198902` |

当前汇总与实验代码已在 Git 提交 `f24d1dd` 中建立可追溯版本；本汇总及阶段六、七新增记录尚待下一次提交。

## 5. 复现入口

| 内容 | 文件 |
| --- | --- |
| MobileNetV2 CPU 基线与转换 | `artifacts/baseline-2026-09-13/阶段一执行记录.md` |
| MindSpore GPU 不兼容证据 | `artifacts/baseline-2026-09-13/阶段二环境兼容性记录.md` |
| ResNet-18 GPU 训练 | `experiments/resnet18_gpu_2026-09-14/train_resnet18_torch.py`、`阶段三执行记录.md` |
| GPU/CPU 同架构对照 | `experiments/resnet18_gpu_2026-09-14/阶段四执行记录.md` |
| ONNX / Lite 导出 | `experiments/resnet18_gpu_2026-09-14/export_onnx.py`、`阶段五执行记录.md` |
| Android 真机 benchmark | `experiments/resnet18_gpu_2026-09-14/阶段六执行记录.md` |

## 6. 结题表述建议

可在结题报告中使用以下结论：

> 项目完成了猫狗图像二分类的 CPU 基线复现、ResNet-18 GPU 训练、同架构 CPU 对照、ONNX 导出与 MindSpore Lite 模型转换。在相同 ResNet-18 训练条件下，GPU 总训练耗时为 200.10 s，CPU 为 1682.46 s，GPU 提速 8.41 倍；最佳验证准确率分别为 74.24% 和 72.22%。ResNet-18 Lite 模型已在 Android 16、SM8650 真机上以 MindSpore Lite 2.9 runtime 成功加载和推理，平均时延为 35.94 ms。

同时必须保留以下说明：MobileNetV2 99.49% 的结果为历史 CPU 基线，不能与 ResNet-18 直接作速度结论；新模型尚未进入旧版 Android App，猫、狗各 10 张独立图片的 App 内准确率测试待获得 Android 源码、升级 Lite 运行时并重建 APK 后完成。

## 7. 提交前自查

- [x] 训练代码、参数、数据划分和环境信息已记录。
- [x] GPU/CPU 同架构对照已明确，不与 MobileNetV2 混算加速比。
- [x] `.pth`、`.onnx`、`.ms` 的哈希已核对。
- [x] ONNX 与 PyTorch 数值一致性已验证。
- [x] 真机 Lite 2.9 模型加载与性能推理已验证。
- [x] MindSpore GPU 不兼容原因已明确写出。
- [ ] 新版 Android App 打包、模型内嵌与 20 张独立图片测试：待 Android 源码。
- [ ] 将本次阶段六、七材料提交并推送到远程仓库：按需执行。
